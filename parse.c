#include "base.h"
#include "defines.h"
#include "settings.h"

char* find_line_contains(char* find, bool reSeek, char* fail, uint32 reSeekStart){
	return "";
}

 PackMetadata get_pack_metadata(FILE* reader){
 	PackMetadata ret;
	return ret;

}